# 차선 검출 파라미터 레퍼런스

작성일: 2026-07-24
대상: src/ws_cam2/src/lane_detection

> 통합 출처: lane_detection_params.md (값을 kcity.yaml 실운영값과 대조 갱신)

`LaneConfig` / `LaneResult` 변수의 의미와 이론적 근거를 정리한다.
**구조체 기본값**은 `include/lane_detection/LaneDetection.hpp`의 초기화값이고, **kcity.yaml 값**은 `yaml/kcity.yaml`에서 런타임에 덮어쓰는 실운영값이다. 두 값이 다르면 실제 동작을 지배하는 것은 kcity.yaml 값이다. 소스 위치는 [04_소스맵.md](04_소스맵.md), 실행 방법은 [01_사용법.md](01_사용법.md), 설계는 [03_설계.md](03_설계.md), 성능 개선은 [05_성능_로드맵.md](05_성능_로드맵.md) 참고.

---

## 0. 좌표계 전제 (왜 이렇게 하나)

차선 인식은 이미지 픽셀이 아니라 **차량 지면 좌표계(m)**에서 다룬다. 카메라 원근 때문에 이미지에서는 평행한 차선이 소실점으로 모여 곡률/폭이 왜곡된다. 그래서 **BEV(IPM, Inverse Perspective Mapping)**로 지면을 위에서 본 평면으로 펴서 미터 단위로 다항식을 피팅한다 → 곡률/차선폭/거리 판정이 물리적 의미를 가진다.

| 파라미터 | 의미 | 구조체 기본값 | kcity.yaml 값 | 비고 |
|---|---|---|---|---|
| `roi_xmin` | 지면 ROI 전방 시작 (m, X+ 전방) | 2.0 | **3.0** | 실운영 하한 상향 |
| `roi_xmax` | 지면 ROI 전방 상한 (m) | 70.0 | **30.0** | 원거리 픽셀 해상도 급감 → 실운영은 30 m로 크게 축소 |
| `roi_ymin` | 지면 ROI 횡 최소 (m, Y+ 우측) | -10.0 | **-4.5** | 실운영 횡폭 축소 |
| `roi_ymax` | 지면 ROI 횡 최대 (m) | 10.0 | **4.5** | 실운영 횡폭 축소 |
| `bev_size` | BEV 이미지 해상도 (px) | 640×640 | 640×640 | ROI를 이 격자에 매핑 |

---

## 1. 세그멘테이션 마스크 전처리 (`DetectFromMask`)

YOLOPv2가 준 차선 마스크를 정제한다.

| 파라미터 | 의미 | 구조체 기본값 | kcity.yaml 값 | 비고 |
|---|---|---|---|---|
| `mask_sobel_thresh` | Sobel-X 이진화 임계 (세로 엣지 추출, 낮을수록 민감·노이즈↑) | 30 | **5** | 실운영은 매우 민감하게 |
| `mask_close_kh` | MORPH_CLOSE 수직 커널 높이 (점선/끊긴 차선 세로 연결) | 9 | 9 | 모폴로지 닫힘 = 팽창 후 침식 |

---

## 2. 색/엣지 기반 전처리 (`preprocess`, 카메라 원본 경로 = Detect())

DetectFromMask 경로에서는 미사용. `Detect()` 원본 이미지 경로 전용.

| 파라미터 | 의미 | 구조체 기본값 | kcity.yaml 값 | 비고 |
|---|---|---|---|---|
| `gamma` | 감마 보정 out=in^(1/γ), 어두운 노면 대비 향상 | 2.0 | 2.0 | |
| `sobel_thres` | Sobel abs 이진화 임계 (세로 엣지 강조) | 40 | 40 | |
| `ksize_vert` | 세로 성분 Open 커널 | 1×7 | 1×7 | |
| `ksize_noise` | 노이즈 성분 Open 커널 | 3×3 | 3×3 | |
| `adaptive_block_size` | 적응형 이진화 블록 크기 (홀수 ≥3) | 15 | 15 | 국소평균 기준 이진화 → 조명 불균일에 강함 |
| `adaptive_C` | 적응형 이진화 offset C | -10.0 | -10.0 | |
| `hls_white_hmin/hmax` | 흰 차선 H 게이트 | 0 / 180 | 0 / 180 | |
| `hls_white_lmin/lmax` | 흰 차선 L 게이트 (흰색=고L) | 70 / 255 | **135** / 255 | lmin 상향 |
| `hls_white_smin/smax` | 흰 차선 S 게이트 (흰색=저S) | 0 / 100 | 0 / **80** | smax 하향 |
| `hls_yellow_hmin/hmax` | 황 차선 H 게이트 | 15 / 35 | 15 / 35 | |
| `hls_yellow_lmin/lmax` | 황 차선 L 게이트 | 80 / 255 | 80 / 255 | HLS가 RGB보다 조명 변화에 강함 |
| `hls_yellow_smin/smax` | 황 차선 S 게이트 (황색=고S) | 80 / 255 | 80 / 255 | |
| `preprocess_adaptive_th` | sobel_abs에 adaptive threshold 적용 | false | 0 | |
| `preprocess_morph_close` | morph close (끊긴 차선 연결) | false | 0 | |
| `preprocess_contour_filter` | contour 기반 소형 블롭 제거 | false | 0 | |
| `ksize_close` | morph close 커널 크기 | 5×5 | (미설정) | |

---

## 3. SearchAround (SA) — 이전 프레임 기반 추적

**이론:** 매 프레임 전체를 훑는 대신 **직전 프레임 곡선 주변 좁은 밴드만** 재탐색 → 빠르고 인접 차선/노이즈 오검출 억제(temporal continuity). 추적 성공 시 기본 모드.

| 파라미터 | 의미 | 구조체 기본값 | kcity.yaml 값 | 비고 |
|---|---|---|---|---|
| `margin_sa` | 이전 곡선 주변 탐색 반폭 (px) | 30 | **10** | 실운영은 밴드 좁게 |
| `safe_gap_multiplier` | fallback safe_gap = margin_sa × multiplier | 2.0 | 2.0 | |
| `sa_seed_use_prev_start` | 이전 계수로 seed 중심 생성 | true | 1 | |
| `sa_seed_track_length_m` | seed 생성 추적 길이 (m) | 8.0 | **12.0** | |
| `sa_seed_count_max` | seed 최대 개수 | 4 | **8** | |
| `sa_init_win_w_px` | SA 초기 창 폭 | 49 | **50** | |
| `sa_init_win_h_px` | SA 초기 창 높이 | 30 | **25** | |
| `sa_win_shrink_w_px_per_step` | 스텝당 창 폭 축소 | 1 | **2** | 원거리로 갈수록 창 축소(원근 반영) |
| `sa_win_shrink_h_px_per_step` | 스텝당 창 높이 축소 | 1 | **2** | |
| `sa_min_win_w_px` | 창 최소 폭 | 10 | **25** | |
| `sa_min_win_h_px` | 창 최소 높이 | 10 | 10 | |
| `lane_edge_min_run_px` | 안쪽 엣지 최소 run 길이 | 2 | 2 | centroid 기준 안쪽 엣지 탐색 |
| `lane_edge_wall_margin_px` | 창 벽 여유 | 0 | 0 | |
| `lane_edge_enable_run_hop` | run-hop 탐색 사용 | true | 1 | |
| `lane_edge_max_hop_gap_px` | 최대 hop 간격 | 12 | 12 | |
| `lane_edge_min_hop_run_width_px` | hop run 최소 폭 | 2 | 2 | |
| `lane_edge_max_hops_per_row` | 행당 최대 hop | 1 | 1 | |
| `prev_seed_start_prior_half_width_px` | 시작상태 prior 반폭 | 25 | 25 | |
| `use_edge_sample_in_sw` | SW에서 edge 샘플 사용 | false | 0 | |

### 3-1. Legacy anchor-SA (구 SA 경로, 호환/튜닝 참조용)

| 파라미터 | 의미 | 구조체 기본값 | kcity.yaml 값 | 비고 |
|---|---|---|---|---|
| `sa_anchor_x_start_m` | 앵커 시작 거리 (m) | 2.0 | 2.0 | |
| `sa_anchor_x_end_m` | 앵커 끝 거리 (m) | 30.0 | 30.0 | |
| `sa_anchor_x_step_m` | 앵커 간격 (m) | 2.0 | 2.0 | |
| `sa_anchor_win_w_px` | 앵커 창 폭 | 28 | **30** | |
| `sa_anchor_win_h_px` | 앵커 창 높이 | 30 | **15** | |
| `sa_anchor_strip_count` | 앵커 내 strip 개수 | 5 | 5 | |
| `sa_anchor_strip_minpix` | strip 최소 픽셀 | 3 | 3 | |
| `sa_anchor_min_valid_points` | 유효 앵커 최소 개수 (미만이면 SW fallback) | 12 | **5** | |
| `sa_anchor_fallback_to_sw` | SA 실패 시 SW 전환 | true | 1 | |

---

## 4. Sliding Window (SW) — 초기/복구 탐색 (현재 strip 방식으로 대체)

**이론:** 이전 정보가 없을 때(초기화/추적 실패) 쓰는 고전 기법. BEV 하단 **히스토그램 좌/우 피크**를 시작점으로 잡고 위로 창을 쌓되 픽셀 무게중심으로 재중심화한다.
**Legacy 참고:** 순수 슬라이딩 윈도우(`computeSlidingWindowBase` + `runSlidingWindow`)는 정의만 있고 현재 파이프라인에서 미사용이다. 실제 복구 경로는 strip 기반(`runStripHistogramGuidedSearch`) / SW v2(`runSwV2`)이다.

| 파라미터 | 의미 | 구조체 기본값 | kcity.yaml 값 | 비고 |
|---|---|---|---|---|
| `n_windows` | 세로 분할 창 개수 | 9 | **10** | |
| `sw_hist_bottom_divisor` | 히스토그램 기준 하단 1/N | 3 | 3 | |
| `margin_sw` | 창 검색폭 | 35 | 35 | |
| `minpix` | 창 재중심 최소 픽셀 수 (적으면 노이즈로 흔들림) | 30 | **3** | 실운영은 매우 낮춤 |
| `win_h` | 창 높이 (px) | 30 | **10** | |
| `win_w` | 창 폭 (px) | 35 | 35 | |
| `sw_forward_limit_m` | SW 탐색 최대 전방 거리 (m, ≤0 비활성) | 25.0 | 25.0 | |
| `min_window_gap` | 좌·우 창 최소 간격 (px) | 150 | 150 | |
| `expected_w` | 기대 차선폭 (px, ≈3.5 m) | 300 | 300 | 한쪽만 검출돼도 반대편 추정 |
| `tolerance` | 허용 오차 (px) | 30 | 30 | |
| `max_shift` | 최대 이동 (px) | 50 | 50 | |
| `sw_use_strip_guided` | strip 기반 대체 탐색 사용 | true | 1 | |
| `strip_count` | BEV 세로 분할 개수 | 12 | 12 | |
| `strip_empty_stop` | 연속 무검출 strip 초과 시 조기종료 | 5 | 5 | |
| `strip_peak_min_value` | strip histogram peak 최소값 | 2550 | 2550 | |
| `strip_noise_min_pixels` | 노이즈 판정 최소 픽셀 | 300 | 300 | 넓게 퍼진 블롭 → 차선과 구분 |
| `strip_noise_growth_ratio` | 직전 strip 대비 급증 배율 | 2.2 | 2.2 | |
| `strip_noise_max_occupancy_ratio` | 히스토그램 점유 폭 비율 상한 | 0.38 | 0.38 | |
| `strip_min_guide_points` | 2차 가이드 피팅 최소 샘플 | 4 | 4 | |
| `strip_band_margin` | 가이드 곡선 밴드 반폭 (px) | 35 | 35 | |
| `blob_max_angle_deg` | StartBlob 방향 각도 변화 허용 (도) | 15.0 | 15.0 | has_prev_start 시에만 적용 |
| `blob_max_pos_delta_px` | StartBlob 시작점 이동 허용 (px) | 20.0 | 20.0 | |

---

## 5. 다항식 피팅 (`fitPolynomial` / `calcLaneCoeffs`)

**이론:** 차선을 3차 다항식 y = ax³ + bx² + cx + d로 근사. 도로 곡선은 **클로소이드(곡률이 거리에 선형)**에 가깝고 3차식이 근거리에서 이를 잘 근사한다. 계수 의미: **d = 횡 offset, c ≈ heading, b ≈ 곡률, a ≈ 곡률변화율.** 계수는 `cv::Vec4d` = **[x³, x², x, 1]** 순, 차량좌표계 횡방향 y.
근거리 가중 최소자승 + 미세 정규화(`kLambda=1e-9`)로 QR 분해(`cv::solve DECOMP_QR`) → 근거리(중요·정확) 점에 큰 가중치, 수치 안정성 확보.

| 파라미터 | 의미 | 구조체 기본값 | kcity.yaml 값 | 비고 |
|---|---|---|---|---|
| `lane_expected_w` | 기대 차선폭 (m) | 3.6 | 3.6 | |

---

## 6. 4축 게이팅 + 검증 (`validateAndSmooth`)

검출 결과를 **NODET / BAD / WEAK / GOOD**로 판정한다 (Validity/Sufficiency/Quality/Temporal).

| 파라미터 | 의미 | 구조체 기본값 | kcity.yaml 값 | 비고 |
|---|---|---|---|---|
| `x_check` | 프레임 간 y 변화 평가 기준 거리 (m) | 5.0 | 5.0 | |
| `span_min_m_weak` | WEAK 최소 span (m) | 5.0 | 5.0 | 짧게만 잡히면 신뢰도↓ |
| `span_min_m_good` | GOOD 최소 span (m) | 10.0 | 10.0 | |
| `rmse_hard_bad_m` | RMSE 치명 하드게이트 (m, 초과→즉시 BAD) | 0.5 | 0.5 | |
| `dy_hard_m` | Δy 치명 하드게이트 (m @x_check) | 1.0 | **1.5** | 실운영 완화 |
| `dkappa_hard` | Δκ 치명 하드게이트 (곡률 변화량) | 0.05 | **0.09** | 실운영 완화 |
| `min_rmse_ratio` | RMSE 신뢰 비율 (작을수록 엄격) | 0.8 | (미설정) | YAML 오버라이드 없음 |
| `lane_w_min` | 물리적 차선폭 하한 게이트 (m) | 2.5 | 2.5 | |
| `lane_w_max` | 물리적 차선폭 상한 게이트 (m) | 5.0 | 5.0 | |
| `quality_min_pixels_good` | GOOD 최소 픽셀 수 | 300 | **1500** | 실운영 대폭 상향 |
| `quality_min_pixels_weak` | WEAK 최소 픽셀 수 | 100 | **300** | |
| `quality_max_rmse_m_good` | GOOD 최대 RMSE (m) | 0.05 | **0.08** | |
| `quality_max_rmse_m_weak` | WEAK 최대 RMSE (m) | 0.15 | **0.18** | |
| `max_bad_frames` | 연속 실패 시 KF 예측 유지 한계 | 10 | 10 | |
| `valid_view_range` | Cam2LD view_range 유효 판정 (m) | 5.0 | 5.0 | |
| `lane_sep_min_m` | 좌/우 최소 간격 게이트 (m) | 1.5 | 1.5 | |
| `lr_slot_by_top_endpoint` | 상단 끝점 기준 좌/우 슬롯 재배치 | true | 1 | |
| `lr_keep_closer_to_mid` | 같은 쪽 충돌 시 중앙 근접 후보 유지 | true | 1 | |
| `weak_to_sw` | 연속 WEAK → SA→SW 전환 임계 | 8 | 8 | 추적 실패 복구 |
| `bad_to_sw` | 연속 BAD → SA→SW 전환 임계 | 3 | 3 | |

---

## 7. 칼만 필터 스무딩 (KF)

**이론:** 프레임마다 계수가 튀는 것을 억제. 상태(계수/샘플)를 시간적으로 추정하고 **측정 신뢰도(R)**에 따라 측정 반영 정도를 조절한다.

| 파라미터 | 의미 | 구조체 기본값 | kcity.yaml 값 | 비고 |
|---|---|---|---|---|
| `kf_R_good` | GOOD 측정노이즈 스칼라 | 0.1 | 0.1 | |
| `kf_R_weak` | WEAK 측정노이즈 스칼라 (약하게 반영) | 1.0 | 1.0 | R↑ → 측정 덜 믿고 예측 유지 |
| `use_geometry_kf` | 기하(y0/θ/κ) KF 사용 | false | 0 | |
| `geom_kf_hold_frames` | geom KF 예측 유지 프레임 | 5 | **15** | |
| `geom_kf_process_noise` | geom KF 프로세스노이즈 Q | 1e-4 | **3.0e-3** | |
| `geom_kf_measurement_noise` | geom KF 측정노이즈 R | 1e-2 | **2.0e-2** | |
| `use_sample_kf_masklite` | 샘플-벡터 KF 사용 | true | **0** | 실운영은 raw fit 경로 사용 |
| `rawfit_hold_frames` | raw fit 예측 유지 프레임 | 5 | **10** | |
| `kf_sample_hold_frames` | 샘플 KF 예측 유지 프레임 | 5 | 5 | |
| `kf_sample_x_m` | 샘플링 거리 목록 (m) | {5,8,12,16} | **{5,8,11,14,25,30}** | 슬롯 수 4→6 |
| `kf_sample_band_half_width_m` | 슬롯 밴드 반폭 (m) | 0.5 | **1.0** | |
| `kf_sample_min_points_per_slot` | 슬롯 최소 점 수 | 1 | 1 | |
| `kf_sample_Q_pos_list` | 위치 프로세스노이즈 | {1e-3 ×4} | **{1e-3,1e-3,1.5e-3,2e-3,3e-3,4e-3}** | |
| `kf_sample_Q_vel_list` | 속도 프로세스노이즈 | {1e-2 ×4} | **{1e-2,1e-2,1.5e-2,2e-2,3e-2,4e-2}** | |
| `kf_sample_R_diag_list` | 슬롯별 측정노이즈 (원거리↑) | {0.3,0.3,0.8,1.2} | **{0.2,0.2,0.3,0.8,1.2,1.8}** | |
| `kf_sample_invalid_R` | 측정 없을 때 큰 R (사실상 예측만) | 100.0 | 100.0 | |

---

## 8. ROI 마스크(사다리꼴) & 컨투어 필터

| 파라미터 | 의미 | 구조체 기본값 | kcity.yaml 값 | 비고 |
|---|---|---|---|---|
| `roi_mask_enabled` | ROI 마스크 적용 여부 | true | **0** | 실운영은 마스크 비활성 |
| `roi_mid_offset_px` | 중심 오프셋 (양수=우측) | 0 | **45** | |
| `roi_half_top_ratio` | 상단(원거리) 반폭 비율 | 0.45 | **0.08** | 실운영 대폭 축소 |
| `roi_half_bot_ratio` | 하단(근거리) 반폭 비율 | 0.35 | **0.15** | 실운영 대폭 축소 |
| `contour_min_area` | 컨투어 최소 넓이 (px²) | 20 | 20 | 작은 블롭 제거 |
| `contour_min_ratio` | 최소 종횡비 (h/w) | 1.5 | 1.5 | 세로로 긴 것만 후보 |
| `contour_merge_dist_x` | 컨투어 병합 가로 거리 (px) | 40 | 40 | |
| `contour_merge_dist_y` | 컨투어 병합 세로 거리 (px) | 20 | 20 | |

---

## 9. RViz 마커 (시각화, 검출 로직 무관)

| 파라미터 | 의미 | 구조체 기본값 | kcity.yaml 값 |
|---|---|---|---|
| `enable_lane_marker` | 마커 발행 여부 | true | 1 |
| `lane_marker_frame_id` | 마커 프레임 | camera_link | camera_link |
| `lane_marker_forward_min_m` | 마커 전방 시작 (m) | 0.0 | 0.0 |
| `lane_marker_forward_max_m` | 마커 전방 끝 (m) | 30.0 | 30.0 |
| `lane_marker_dx_m` | 샘플 간격 (m) | 0.5 | 0.5 |
| `lane_marker_line_width_m` | 선 두께 (m) | 0.12 | 0.12 |
| `lane_marker_origin_radius_m` | 원점 반경 (m) | 0.25 | 0.25 |
| `lane_marker_z_m` | 마커 높이 (m) | 0.02 | 0.02 |
| `color_roi` | drawROI 색 | (0,255,0) | [0,255,0] |
| `thickness_roi` | drawROI 두께 | 2 | 2 |

---

## 10. LaneResult (프레임 출력)

| 필드 | 의미 |
|---|---|
| `coeffs_fit_L/R` | 피팅 직후 raw 계수 (품질 평가 기준) |
| `coeffs_L/R` | **KF 후 최종 계수 (발행/그리기용)** |
| `detected_fit_L/R` | 피팅 성공 여부 |
| `is_detected_L/R` | 검출 성립 여부 (true=검증 통과, false=NODET·계수 0) |
| `state_L/R` | NODET/BAD/WEAK/GOOD 최종 상태 |
| `quality_L/R` | GOOD/WEAK/BAD 품질 (하위호환) |
| `rmse_fit_m_L/R` | 미터 단위 피팅 RMSE |
| `pixel_count_L/R` | 사용 픽셀 수 |
| `hard_fail_L/R` | 치명 하드게이트 위반 여부 |
| `lane_start_m_L/R` | 차선 시작 x (m, near) |
| `lane_end_m_L/R` | 차선 끝 x (m, far) |
| `span_m_L/R` | 검출 커버리지 (x, m) |
| `view_range_m_L/R` | Cam2LD view range (m) |
| `availability_L/R` | Cam2LD availability (0/1) |
| `delta_y_m_L/R` | 이전 프레임 대비 Δy @x_check |
| `delta_kappa_L/R` | 곡률 변화량 (3차식 기반) |

---

## 파이프라인 한눈에

```
lane_mask (YOLOPv2)
  └─ DetectFromMask
       ├─ (마스크 전처리: Sobel-X, MORPH_CLOSE)
       ├─ findPixelsFromMask → pixels_L_/R_   (SA 추적 / SW·strip 복구)
       └─ fitPolynomial → calcLaneCoeffs       (3차 가중최소자승, QR)
            → coeffs_fit_L/R
            → validateAndSmooth (4축 게이팅: RMSE/span/Δy/Δκ)
            → 칼만 스무딩 (품질별 R, 샘플-벡터 KF)
            → coeffs_L/R  +  state_L/R
```

> 주의: 모든 파라미터 이름은 현재 구조체에 그대로 존재한다(삭제된 항목 없음). 값이 다를 때는 kcity.yaml 값이 실제 동작을 지배한다.
